<div class="row">
  <div class="small-7 columns">
    <%= render partial: "flavor_details" %>


<!-- Add buttons to list all, edit project or destroy object -->
	<%if logged_in? and current_user.role? :admin%>
		<%= render partial: "partials/object_options", locals: {model_name: "flavor", object: @flavor}  %>


	<% else %>
	    <div class="form-actions">
	      <%= link_to(jobs_path, class: 'button radius') do %>
	        &nbsp;Back to Flavors
	      <% end %>
	    </div>
	<% end %>

  </div>
</div>