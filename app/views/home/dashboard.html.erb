<h2> Hello, <%= current_user.employee.first_name %></h2>
<h2><%= Date.current.strftime("%A %B %d, %Y") %></h2>	
<h2><%= @store.name %></h2>	


<div class="row">
 <!--  <div class="small-6 columns">
		<%# @thisStore = Assignment.current.for_employee(current_user.employee_id).first.store %>
		<h3>Store Details</h3>
		<p>
		  <h4>
		    Name: <%#= @thisStore.name %>
		  </h4>
		</p>

		<p>
		  <b>Address:</b><br>
		  <%#= @thisStore.street %><br />
		  <%#= @thisStore.city %>, <%#= @thisStore.state %> <%#= @thisStore.zip %>
		</p>

		<p>
		  <b>Phone:</b><br>
		  <%#= number_to_phone @thisStore.phone %>
		</p>

		<p>
		  <b>Active:</b> 
		  <%#= @thisStore.active ? "Yes" : "No" %>
		</p> 
  </div> -->

  	<div class="small-6 columns">

		<% if @today_shifts.empty? %>
		  <p><em>There are no shifts scheduled for today.</em></p>
		<% else %>
		  <h3>Today's Shifts</h3>
		  <table class="table table-striped">
		    <thead>
		      <tr>
		        <th>Employee</th>
		        <th>Start Time</th>
		        <th>End Time</th>
		        <th>Phone</th>
		        <th>&nbsp;</th>


		      </tr>
		    </thead>
		    <tbody>
		      <% @today_shifts.each do |shift| %>
		      
		        <tr>
		          <td><%= link_to shift.assignment.employee.proper_name, employee_path(shift.assignment.employee) %></td>
		          <td><%= shift.start_time.localtime.strftime("%I:%M %p") %></td>
		          <td><%= shift.end_time.localtime.strftime("%I:%M %p") %></td>
				  <td><%= number_to_phone shift.assignment.employee.phone unless shift.assignment.employee.phone.nil?  %></td>
		          <%if shift.end_time.localtime.strftime("%I:%M %p") < Time.now.strftime("%I:%M %p") %>
			          <td>
			            <%= link_to(shift_path(shift), class: 'button tiny radius warning') do %>
			              &nbsp;Add Jobs&nbsp;
			            <% end %>
	          		  </td>
	          	  <% else %>
	          	       <td>&nbsp;</td>
			      <% end %>
		        </tr>
		      <% end %>
		    </tbody>
		  </table>
		  <%= will_paginate @today_shifts, previous_label: "Previous&nbsp;", next_label: "&nbsp;Next" %>  
		<% end %>
		
	</div>
  
    <div class="small-6 columns">
	  <%= render 'stores/list_of_flavors' %>

	  <%= link_to(new_store_flavor_path(store_id: @store.id), id: 'new_store_flavor_link', class: 'button tiny radius', remote: true) do %>
	    Add New Flavor
	  <% end %>
	</div>

	<div class="small-12 columns">
		<%# @storeassignments = Assignment.current.for_store(@store).paginate(page: params[:page]).per_page(15) %>
		<% state = "current" %> 

		<% if @assignments.empty? %>
		  <p><em>There are no <%= state %> assignments at this time.</em></p>
		<% else %>

		  <h3><%= state.capitalize %> Assignments</h3>
		  <table class="table table-striped">
		    <thead>
		      <tr>
		        <th>Employee</th>
		        <th>Phone</th>
		        <th class="centered">Role</th>
		        <th>&nbsp;</th>

		      </tr>
		    </thead>
		    <tbody>
		      <% @assignments.each do |assignment| %>
		      <%# @assignment_test = assignment %>
		        <tr>
		          <td><%= link_to assignment.employee.name, employee_path(assignment.employee) %></td>
		          <td><%= number_to_phone assignment.employee.phone unless assignment.employee.phone.nil?  %></td>
		          <td class="centered"><%= assignment.employee.role.capitalize %></td>
		          <td>
		            <%= link_to(assignment_path(assignment), class: 'button tiny radius warning') do %>
		              &nbsp;View Details&nbsp;
		            <% end %>
	          	  </td>

		        </tr>
		      <% end %>
		    </tbody>
		  </table>
		  <%= will_paginate @assignments, previous_label: "Previous&nbsp;", next_label: "&nbsp;Next" %>  
		<% end %>
	</div>
</div>



<!-- 	<div class="small-6 columns">
		<%#= render partial: "shifts/list_shifts", locals: {store: @store, shifts: @shifts}  %>
		<%#= render "list_shifts", locals: {model_name: "store", object: @thisStore}  %>
		<%#= render 'shifts/list_shifts' %>
 	</div> -->


  


<!-- Add buttons to list all, edit project or destroy object -->
<%# render partial: "partials/object_options", locals: {model_name: "job", object: @job}  %>